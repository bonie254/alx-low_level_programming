#include <unistd.h>
#include "main.h"

/**
 * _putchar - writes the character c to the std output
 * c: the character to print
 * Return: on succes 1 else -1
 */

int _putchar(char c)
{
	return (write(1, &c, 1));
}
